# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local secrets / machine config
config.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust
src-tauri/target

# dictionary build cache (large downloads)
scripts/.cache/

# 词块(chunks)构建：本地一次性管线 + 原始 vendor 源 + 中间产物；只把成品 src/data/chunks.json 入库
chunks-vendor/
chunks.seed.json

# WorkBuddy workspace data
.workbuddy/
